package main

func zigzagLevelOrder(root *TreeNode) [][]int {
	res := make([][]int, 0)
	queue := make([]*TreeNode, 0)
	queue = append(queue, root)
	left := true
	for len(queue) != 0 {
		if left == true {

		}
	}
	return res
}
