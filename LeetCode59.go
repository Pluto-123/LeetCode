package main

// fail
func generateMatrix(n int) [][]int {
	//// init
	//res := make([][]int, n)
	//for i := 0; i < len(res); i++ {
	//	res[i] = make([]int, n)
	//}
	//// 边界
	//top := 0
	//bottom := n - 1
	//left := 0
	//right := n - 1
	//trace := make([]int, 2)
	//for i := 0; i < n*n; i++ {
	//	res[trace[0]][trace[1]] =
	//}
	return nil
}
